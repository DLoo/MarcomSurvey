@model MarcomSurvey.Models.GuestResponse

@{
    ViewBag.Title = "tryFormPost";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
 @*   <link rel="stylesheet" type="text/css" href="~/Contents/Site.css" />*@
    <title>tryFormPost</title>
</head>
<body>
   @using (Html.BeginForm(null, null, FormMethod.Post, new { @action = "http://localhost:53009/home/postData/IT/SurveyA" }))
    {
    
        @Html.EditorForModel()
        <input type="submit" value="Submit RSVP" />

    }

@*
    @using (Html.BeginForm()) {
        @Html.ValidationSummary();
        <br/>
		<p>Your name: @Html.TextBoxFor(x => x.Name) </p>
		<p>Your email: @Html.TextBoxFor(x => x.Email) </p>
		<p>Your phone: @Html.TextBoxFor(x => x.Phone) </p>
		<p>
		     Will you attend?
		     @Html.DropDownListFor(x => x.WillAttend, new[] {
		          new SelectListItem()  { Text = "Yes, I’ll be there", Value = bool.TrueString },
		          new SelectListItem()  { Text = "No, I can’t come", Value = bool.TrueString }
		     }, "Choose an option")
		</p>
		<input type="submit" value="Submit RSVP" />
	}
    *@
 
</body>
</html>
